fastapi==0.137.1
uvicorn==0.49.0
pydantic==2.13.4
requests==2.34.2

# Optional — only needed if you want real Firestore instead of the local JSON
# DB fallback. Adds real time to Render's free-tier build. Skip for now;
# the app already degrades gracefully without these.
# firebase-admin
# google-cloud-firestore
